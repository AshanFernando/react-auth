---
description: 'A shell-app app.'
labels: ['app', 'react']
---

A React application for shell-app. 

## Run the application

Import the app and add the following config to your workspace.jsonc:

```
bit use shell-app 
```

To run the app locally, run the following:

```
bit run shell-app 
```
